import numpy as np
from sklearn.metrics import mean_squared_error
import pandas as pd 

#df_test = pd.read_csv("df_test.csv")

def validate_df(df):
    # Validate columns and index of df
    return True

def score_df(df):
    # Calculate nrmse of dataframe
    return np.random.random()